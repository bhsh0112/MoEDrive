# 多模态MoE训练优化总结

## 优化前后对比

| 配置项 | 原始值 | 优化值 | 说明 |
|--------|--------|--------|------|
| **损失权重** |
| `trajectory_mode_weight` | 1.0 | 2.0 | 加强模式分类学习 |
| `moe_aux_loss_weight` | 0.5 | 0.3 | 避免过度正则化 |
| `trajectory_heading_weight` | 1.0 | 1.5 | 航向更难学习，增加权重 |
| **MoE配置** |
| `moe_load_balance_coef` | 5e-3 | 1e-2 | 加强专家负载均衡 |
| **训练策略** |
| `max_epochs` | 100 | 150 | 增加训练轮数 |
| `gradient_clip_val` | 无 | 1.0 | 梯度裁剪，提高稳定性 |
| `gradient_clip_algorithm` | 无 | norm | 使用梯度范数裁剪 |

## 优化脚本对比

### 1. train_moe_multimodal_optimized.sh（推荐）

保持20个专家，应用所有优化：
- ✅ 损失权重优化
- ✅ MoE正则化优化
- ✅ 梯度裁剪
- ✅ 增加训练轮数

**适用场景**：当前20个专家配置良好时使用

### 2. train_moe_multimodal_v2.sh

减少到16个专家，其他优化相同：
- ✅ 专家数量：20 → 16
- ✅ 轨迹模式数量：20 → 16
- ✅ 其他优化与optimized版本相同

**适用场景**：20个专家导致负载不均衡时使用

## 快速开始

### 使用优化脚本1（20个专家）

```bash
bash scripts/train_moe_multimodal_optimized.sh
```

### 使用优化脚本2（16个专家）

```bash
bash scripts/train_moe_multimodal_v2.sh
```

## 预期效果

### 1. 损失权重优化
- **trajectory_mode_weight**: 2.0 → 模式分类更准确
- **moe_aux_loss_weight**: 0.3 → 减少过度正则化，提高模型容量利用
- **trajectory_heading_weight**: 1.5 → 航向预测更准确

### 2. MoE负载均衡优化
- **moe_load_balance_coef**: 1e-2 → 专家使用更均衡

### 3. 训练稳定性
- **梯度裁剪**: 防止梯度爆炸
- **增加训练轮数**: 给模型更多学习机会

## 监控建议

训练过程中重点关注：

1. **模式分类损失** (`trajectory_mode_loss`)
   - 应该稳步下降
   - 如果上升或不下降，可能需要进一步调整 `trajectory_mode_weight`

2. **专家使用分布** (`moe_usage_fraction_e{i}`)
   - 所有专家应该都有一定使用率（> 2-3%）
   - 如果某些专家使用率接近0，考虑使用v2脚本（16个专家）

3. **总损失** (`loss`)
   - 应该稳定下降，不应有大幅波动
   - 如果有波动，可能需要降低学习率或增加梯度裁剪阈值

4. **验证损失** (`val_loss`)
   - 监控过拟合情况
   - 如果验证损失不下降或上升，考虑添加更多正则化

## 进一步优化方向

如果优化后效果仍不理想，可以考虑：

1. **学习率调整**
   - 使用学习率调度器（余弦退火）
   - 添加学习率预热

2. **架构调整**
   - 调整MoE专家数量（12, 16, 20, 24）
   - 调整Transformer层数或隐藏层维度

3. **数据增强**
   - 增加数据增强策略
   - 使用更丰富的训练数据

4. **评估指标**
   - 添加MinADE, MinFDE等多模态评估指标
   - 可视化不同模式的轨迹分布

## 注意事项

1. **从checkpoint继续训练**：如果要从之前的checkpoint继续训练，需要确保配置兼容（特别是专家数量）

2. **评估脚本更新**：使用优化后的模型评估时，确保评估脚本中的配置与训练时一致

3. **资源消耗**：优化后的训练可能需要更多GPU内存，确保有足够的资源

4. **实验记录**：建议记录每次实验的配置和结果，方便对比分析

