# 多模态轨迹预测实现 - 测试指南

## 概述

本文档提供了多模态轨迹预测实现的测试方案和运行指南。

## 测试脚本

### 1. 快速测试 (`test_multimodal_quick.py`)

快速验证基本功能，适合快速检查实现是否正确。

```bash
cd /data2/file_swap/sh_space/MOEDrive
python test_multimodal_quick.py
```

**测试内容:**
- 模型初始化
- 基本前向传播
- 输出形状验证
- 数值有效性检查

**预期输出:**
- 模型成功创建
- 前向传播无错误
- 输出包含 `trajectory`, `trajectory_modes`, `trajectory_mode_scores`

### 2. 完整测试套件 (`test_multimodal_trajectory.py`)

全面的测试套件，包括所有功能的详细测试。

```bash
cd /data2/file_swap/sh_space/MOEDrive
python test_multimodal_trajectory.py
```

**测试内容:**

#### 测试1: 模型初始化
- ✓ 单模态模式初始化
- ✓ 多模态模式初始化
- ✓ 配置一致性验证

#### 测试2: Forward Pass 输出形状验证
- ✓ 单模态输出形状: `(B, num_poses, 3)`
- ✓ 多模态输出形状:
  - `trajectory` (best): `(B, num_poses, 3)`
  - `trajectory_modes`: `(B, num_modes, num_poses, 3)`
  - `trajectory_mode_scores`: `(B, num_modes)`

#### 测试3: 损失函数计算
- ✓ 单模态损失计算
- ✓ 多模态损失计算
- ✓ 损失数值合理性验证

#### 测试4: 梯度检查
- ✓ 单模态模式梯度
- ✓ 多模态模式梯度
- ✓ 梯度数值有效性

#### 测试5: 数值稳定性
- ✓ 极端输入处理（大数值/小数值）
- ✓ NaN/Inf 检测
- ✓ 多次前向传播一致性

#### 测试6: 模式多样性检查
- ✓ 不同模式之间的差异检查
- ✓ 模式多样性评估

## 测试前准备

### 1. 环境检查

确保以下依赖已安装:
```bash
# PyTorch
torch >= 1.9.0

# NumPy
numpy >= 1.19.0

# NuPlan (用于 TrajectorySampling)
# 需要根据实际环境安装
```

### 2. 路径配置

测试脚本已配置相对路径，但如果需要，可以修改：
```python
sys.path.insert(0, '/data2/file_swap/sh_space/MOEDrive')
```

### 3. 配置验证

确保以下配置参数正确：
- `moe_num_experts == num_trajectory_modes` (多模态模式下)
- `use_moe_decoder == True` (使用MoE decoder)
- `multimodal_trajectory == True` (启用多模态)

## 常见问题排查

### 问题1: Import 错误

**错误信息:**
```
ModuleNotFoundError: No module named 'navsim'
```

**解决方案:**
```bash
# 确保在正确的目录下运行
cd /data2/file_swap/sh_space/MOEDrive

# 或者添加路径到 PYTHONPATH
export PYTHONPATH=/data2/file_swap/sh_space/MOEDrive:$PYTHONPATH
```

### 问题2: CUDA 内存不足

**错误信息:**
```
RuntimeError: CUDA out of memory
```

**解决方案:**
- 在测试脚本中减小 `batch_size`
- 减小模型尺寸 (`tf_d_model`, `tf_d_ffn`)
- 减少 `num_bounding_boxes` 和 `num_trajectory_modes`

### 问题3: 形状不匹配

**错误信息:**
```
RuntimeError: Shape mismatch
```

**解决方案:**
- 检查配置参数是否一致
- 确保 `moe_num_experts == num_trajectory_modes`
- 检查输入特征的形状是否正确

### 问题4: 梯度为 None

**错误信息:**
```
没有检测到有效梯度
```

**解决方案:**
- 确保模型处于 `train()` 模式
- 检查损失函数是否正确反向传播
- 验证模型参数是否被正确初始化

## 测试结果解读

### 成功输出示例

```
================================================================
测试1: 模型初始化
================================================================
   ✓ 单模态模型初始化成功
   ✓ 多模态模型初始化成功
   ✓ 配置一致性验证通过

================================================================
测试2: Forward pass 输出形状验证
================================================================
   ✓ 单模态输出形状正确: torch.Size([2, 8, 3])
   ✓ 轨迹模式形状正确: torch.Size([2, 8, 8, 3])
   ✓ 模式分数形状正确: torch.Size([2, 8])
   ✓ 多模态输出形状验证通过

================================================================
测试结果汇总
================================================================
模型初始化: ✓ 通过
Forward pass 形状验证: ✓ 通过
损失函数计算: ✓ 通过
梯度检查: ✓ 通过
数值稳定性: ✓ 通过
模式多样性检查: ✓ 通过

================================================================
✓ 所有测试通过！
================================================================
```

### 失败输出解读

如果某个测试失败：
1. **查看具体错误信息** - 错误信息会指出失败的具体位置
2. **检查配置** - 确保所有配置参数正确
3. **检查形状** - 验证张量形状是否匹配预期
4. **检查数值** - 确认没有 NaN 或 Inf

## 性能基准

在测试环境中，预期性能指标：

| 测试项 | 预期值 |
|--------|--------|
| 模型初始化时间 | < 1秒 |
| 单次前向传播时间 | < 0.5秒 (CPU) / < 0.1秒 (GPU) |
| 内存使用 | < 2GB (batch_size=2) |
| 损失计算时间 | < 0.1秒 |

## 下一步

测试通过后，建议：

1. **集成测试**: 在实际训练脚本中测试
2. **超参数调优**: 调整损失权重和路由参数
3. **可视化**: 添加轨迹可视化工具
4. **端到端训练**: 在小数据集上进行完整训练测试

## 联系与反馈

如遇到问题，请检查：
1. 代码版本是否最新
2. 配置参数是否正确
3. 依赖版本是否兼容

